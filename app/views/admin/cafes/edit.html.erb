<% content_for :page_title, "カフェ編集" %>
<% return_to = params[:return_to].to_s %>
<% safe_back_path = return_to.start_with?("/") && !return_to.start_with?("//") ? return_to : cafe_path(@cafe) %>
<% content_for :back_path, safe_back_path %>

<%= render "form", cafe: @cafe, prefectures: @prefectures, tags: @tags, submit_label: "更新する", cancel_path: safe_back_path %>
