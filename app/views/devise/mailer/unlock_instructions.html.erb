<p><%= t("views.devise.mailer.unlock_instructions.greeting", email: @resource.email) %></p>

<p><%= t("views.devise.mailer.unlock_instructions.message") %></p>

<p><%= t("views.devise.mailer.unlock_instructions.instruction") %></p>

<p><%= link_to t("views.devise.mailer.unlock_instructions.action"), unlock_url(@resource, unlock_token: @token) %></p>
